class Project {
  constructor(
    id,
    title,
    category,
    goal,
    note,
    startDate,
    endDate,
  ) {
    this.id = id;
    this.title = title;
    this.category = category;
    this.goal = goal;
    this.note = note;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}

export default Project;
